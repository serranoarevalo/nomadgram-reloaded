import React from "react";
import PropTypes from "prop-types";
import styled from "styled-components";

const SignUpPresenter = () => "Presenter";

SignUpPresenter.propTypes = {};

export default SignUpPresenter;
